torch
torchvision
numpy
scipy
trimesh
tqdm
geomstats

# geomfum and PyRMT have additional, package-specific installation steps,
# see the "Installation" section of the README -- do not install them with
# `pip install geomfum` / `pip install PyRMT`.
